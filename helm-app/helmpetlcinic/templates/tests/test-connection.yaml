apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmpetlcinic.fullname" . }}-test-connection"
  labels:
    {{- include "helmpetlcinic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmpetlcinic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
